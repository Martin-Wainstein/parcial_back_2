eureka:
  client:
    enabled: false

Server:
  port: 8086

spring:
  application:
    name: users-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/usersdb
      username: admin
      password: admin
      database: parcial_backend_2
      port: 27017
      host: mongodb

  main:
    allow-circular-references: true
  security:
    oauth2:
      client:
        provider:
          keycloak-provider:
            issuer-uri: http://localhost:8082/realms/digitalmedia
        registration:
          api-movies-service:
            provider: keycloak-provider
            client-id: microservicios
            client-secret: gI6sb58U6cieJkGLZnFuWxKnOFCpzxDl
            authorization-grant-type:
            redirect-uri: http://localhost:8084/*

springdoc:
  show-actuator: true
  swagger-ui:
    groups-order: DESC
    disable-swagger-default-url: true
management:
  endpoints:
    web:
      exposure.include: beans, env, health, info, metrics, mappings
  endpoint:
    health:
      show-details: always

app:
  cors:
    allowed-origins: http://localhost:3000